<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/subscriber_picture"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/nav_header_desc"
        android:paddingTop="@dimen/nav_header_vertical_spacing"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@mipmap/ic_launcher_round" />

    <TextView
        android:id="@+id/subscriber_full_name"
        android:layout_width="246dp"
        android:layout_height="75dp"
        android:layout_marginStart="8dp"
        android:paddingVertical="10dp"
        android:text="TextView"
        android:textAlignment="center"
        android:textSize="20sp"
        app:layout_constraintBottom_toBottomOf="@+id/imageView"
        app:layout_constraintStart_toEndOf="@+id/subscriber_picture"
        app:layout_constraintTop_toTopOf="@+id/subscriber_picture" />

    <Button
        android:id="@+id/invite_btn"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="8dp"
        android:paddingVertical="0dp"
        android:pointerIcon="arrow"
        android:text="+"
        android:textSize="40sp"
        app:layout_constraintBottom_toBottomOf="@+id/subscriber_full_name"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@+id/subscriber_full_name"
        app:layout_constraintTop_toTopOf="@+id/subscriber_full_name" />

</android.support.constraint.ConstraintLayout>